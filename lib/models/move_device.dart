class MoveDevice {
  String image;
  String title;
  String subtitle;

  MoveDevice(this.image, this.title, this.subtitle);
}

class MoveLocation {
  String title;
  bool isSelected;

  MoveLocation(this.title, this.isSelected);
}
